/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add options here if you actually need them.
  // Example for Docker: output: 'standalone'
  // Example for Netlify/CF Pages (no Image Optimization server): images: { unoptimized: true },
};

module.exports = nextConfig;